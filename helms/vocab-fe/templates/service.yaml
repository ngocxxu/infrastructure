apiVersion: v1
kind: Service
metadata:
  name: {{ include "vocab-fe.fullname" . }}
  labels:
    {{- include "vocab-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vocab-fe.selectorLabels" . | nindent 4 }}
