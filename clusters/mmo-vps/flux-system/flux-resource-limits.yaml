# ============================================================
# Flux controller resource right-sizing
# ============================================================
# gotk-components.yaml ships every controller with a hardcoded
# 1Gi / 1000m limit. On this single node (3 CPU / 4.7Gi allocatable)
# that alone reserved 6Gi of memory limits and 6 CPU of limits —
# the bulk of the "limits are higher than node capacity" warning.
#
# Real observed usage:
#   image-reflector-controller   160Mi   15m
#   kustomize-controller          66Mi   11m
#   source-controller             50Mi    2m
#   helm-controller               42Mi    1m
#   image-automation-controller   30Mi    1m
#   notification-controller       27Mi    2m
#
# Limits below give ~2-4x headroom over observed peak.
#
# NOTE: patched here rather than edited in gotk-components.yaml,
# because that file is regenerated by `flux bootstrap` and any
# direct edit would be silently reverted on the next upgrade.
#
# BONUS: the controllers set GOMEMLIMIT from limits.memory via
# resourceFieldRef, so lowering the limit also tightens Go's GC
# target — they will actually hold less RSS, not just be capped.
# ============================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: source-controller
  namespace: flux-system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kustomize-controller
  namespace: flux-system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              cpu: 25m
              memory: 80Mi
            limits:
              cpu: 500m
              memory: 120Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helm-controller
  namespace: flux-system
spec:
  template:
    spec:
      containers:
        - name: manager
          # OOMKilled at 100Mi right after this repo added two new
          # HelmReleases (rabbitmq, debezium-operator) in one push.
          # 64-100Mi was sized from IDLE steady-state - actively
          # installing/upgrading a chart makes helm-controller render
          # the whole chart template tree in memory, which is a real,
          # transient spike well above idle. 100Mi has no room for that.
          # 180Mi covers install/upgrade bursts; drop back toward 100Mi
          # later if this repo settles into "no new charts most weeks".
          resources:
            requests:
              cpu: 25m
              memory: 80Mi
            limits:
              cpu: 500m
              memory: 180Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-controller
  namespace: flux-system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              cpu: 25m
              memory: 48Mi
            limits:
              cpu: 300m
              memory: 80Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-reflector-controller
  namespace: flux-system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              cpu: 25m
              memory: 176Mi
            limits:
              cpu: 500m
              memory: 320Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-automation-controller
  namespace: flux-system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              cpu: 25m
              memory: 48Mi
            limits:
              cpu: 300m
              memory: 80Mi
