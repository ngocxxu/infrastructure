# Monitoring Layer - Weave GitOps UI
# Depends on infrastructure (Doppler Operator for secrets)
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-monitoring
  namespace: flux-system
spec:
  interval: 1m
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./clusters/mmo-vps/apps/monitoring
  dependsOn:
    - name: infrastructure # Wait for Doppler Operator to be installed
  prune: true
  wait: true
