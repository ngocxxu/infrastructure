apiVersion: v1
kind: Service
metadata:
  name: {{ include "vocab-be.fullname" . }}
  labels:
    {{- include "vocab-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vocab-be.selectorLabels" . | nindent 4 }}
