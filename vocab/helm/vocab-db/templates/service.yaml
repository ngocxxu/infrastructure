apiVersion: v1
kind: Service
metadata:
  name: {{ include "vocab-db.fullname" . }}
  labels:
    {{- include "vocab-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vocab-db.selectorLabels" . | nindent 4 }}
